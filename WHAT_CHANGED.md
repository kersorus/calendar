# Version 0.5 fix

Исправления поверх версии 0.5:

- исправлена ошибка сборки в `MainActivity.java`: переносы строк в `AlertDialog.setMessage()` теперь экранированы как `\n`;
- `apply_update.sh` теперь также копирует `WHAT_CHANGED.md` в корень проекта;
- сохранены изменения 0.5: окно дня по нажатию на дату календаря, добавление записи на выбранную дату, редактирование записей дня, ограничение ручных записей максимум 24 часа в сутки.
