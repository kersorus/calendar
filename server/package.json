{
  "name": "las-calendar-cloudflare-worker",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "find src test -type f -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.102.0"
  }
}
